<Window x:Class="ClaudeBar.UI.FloatingMeterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ClaudeBar.UI"
        Title="ClaudeBar"
        Width="168" Height="36"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        SizeToContent="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#B3101014"
            CornerRadius="8"
            Padding="0"
            BorderBrush="#33FFFFFF"
            BorderThickness="1">
        <Border.ContextMenu>
            <ContextMenu x:Name="RootContextMenu">
                <MenuItem x:Name="LockMenuItem"
                          Header="Lock position"
                          IsCheckable="True"
                          Click="LockMenuItem_Click"/>
                <Separator/>
                <MenuItem Header="Refresh now" Click="RefreshMenuItem_Click"/>
                <MenuItem Header="Settings…" Click="PrefsMenuItem_Click"/>
                <Separator/>
                <MenuItem Header="Hide meter" Click="HideMenuItem_Click"/>
            </ContextMenu>
        </Border.ContextMenu>

        <local:UsageBarsControl x:Name="Bars"
                                Margin="0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"/>
    </Border>
</Window>
